```

D:\竹相左边20250219\ai_work\news0429
├── core/                     # 存放核心逻辑代码
│   └── extractor.py          # 负责从输入文本中提取信息的函数
├── mappings/                 # 存放映射文件的文件夹
│   ├── european_countries.py # 存储欧洲国家代码到名称的映射
│   └── us_states.py          # 存储美国州份缩写到全称的映射
├── utils/                    # 存放实用工具模块的文件夹 (目前可能为空)
├── venv/                     # Python 虚拟环境，包含项目依赖的库
├── .gitignore                # 指定 Git 应该忽略的文件和文件夹
├── input_data.txt            # 一个空文件，可能用于存放输入数据 (目前未使用)
├── LICENSE                   # 项目的许可证文件
├── main_gui.py               # 包含 GUI 主程序的 Tkinter 脚本
├── README.md                 # 项目的说明文档 (Markdown 格式)
└── requirements.txt          # 列出项目依赖的 Python 包及其版本
```