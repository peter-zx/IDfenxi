```

your_async_tool_gui/
├── main_gui.py         # 主要的 GUI 脚本，包含异步逻辑和 UI 更新
├── core/               # 存放核心的业务逻辑
│   └── extractor.py    # 负责异步信息提取的模块
└── utils/              # 存放一些工具函数
│   └── helpers.py      # (可选) 辅助函数

```